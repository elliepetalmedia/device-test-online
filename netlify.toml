[build]
  command = "npm install && npx vite build --config vite.config.ts"
  publish = "dist/public"
  
[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
